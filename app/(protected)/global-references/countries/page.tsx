import { GlobalReferencePageImpl } from '@/app/(protected)/global-references/_components/global-reference-page-impl';

export default function CountriesPage() {
  return <GlobalReferencePageImpl screen="countries" />;
}
